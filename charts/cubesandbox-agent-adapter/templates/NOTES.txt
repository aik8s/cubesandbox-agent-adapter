CubeSandbox Agent Adapter is installed.

Service URL inside this namespace:
  http://{{ include "cubesandbox-agent-adapter.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}

Label each in-namespace Runtime Pod that may call the Adapter:
  cubesandbox-agent-adapter-client: "true"

State mode: {{ ternary "Redis-backed HA/recovery" "in-memory single replica" .Values.state.enabled }}
Audit storage: {{ ternary "persistent volume" "ephemeral emptyDir" .Values.audit.persistence.enabled }}

Export audit events to a centralized sink and configure CubeAPI authentication
before using this deployment beyond a protected test environment.
